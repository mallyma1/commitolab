# Environment Variables for Local Development

# Required for Client (Expo)
# ================================
# API domain for the Express server
EXPO_PUBLIC_DOMAIN=localhost:5000

# Required for Server
# ================================
# PostgreSQL database connection string
# For local dev, you can use a local Postgres instance or a cloud database
DATABASE_URL=postgresql://user:password@localhost:5432/commitolab

# Server port (optional, defaults to 5000)
PORT=5000

# Optional: External Services
# ================================
# Stripe (for subscription features - can be skipped for basic development)
# REPLIT_CONNECTORS_HOSTNAME=
# REPL_IDENTITY=
# WEB_REPL_RENEWAL=

# Twilio (for SMS verification - can be skipped for email-only auth)
# Twilio credentials fetched via Replit connector in production
# For local dev, SMS auth will be disabled

# OpenAI (for onboarding AI features - will use fallbacks if not set)
# OPENAI_API_KEY=your_openai_api_key_here
# OPENAI_MODEL_PROFILE=gpt-4o-mini
# OPENAI_MODEL_RECS=gpt-4o-mini

# Object Storage (Replit-specific, can be skipped for local dev)
# PUBLIC_OBJECT_SEARCH_PATHS=
# PRIVATE_OBJECT_DIR=

# Development mode
NODE_ENV=development
FREE_MODE=true
