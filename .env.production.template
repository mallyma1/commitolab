# Production Environment Variables
# =================================
# Copy this file to .env.production and fill in real values before deployment
# DO NOT commit .env.production to git (it's .gitignored)

# Required for Client (Expo)
# ================================
# Production API domain (e.g., api.commito.app or your-server.fly.dev)
EXPO_PUBLIC_DOMAIN=YOUR_PRODUCTION_API_DOMAIN

# Required for Server
# ================================
# Production PostgreSQL database URL
# Example formats:
#   - Heroku: postgres://user:pass@host:5432/dbname
#   - Neon: postgres://user:pass@ep-xxx.us-east-2.aws.neon.tech/dbname
#   - Supabase: postgres://postgres:pass@db.xxx.supabase.co:5432/postgres
DATABASE_URL=YOUR_PRODUCTION_DATABASE_URL

# Server port (usually provided by hosting platform)
PORT=5000

# Node environment (must be "production")
NODE_ENV=production

# Subscription mode (set to false to enable paid features)
FREE_MODE=false

# Required: OpenAI API
# ================================
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-xxx

# Optional: Override default models (defaults to gpt-4o-mini)
# OPENAI_MODEL_PROFILE=gpt-4o-mini
# OPENAI_MODEL_RECS=gpt-4o-mini

# Required: Stripe (Payment Processing)
# ================================
# Get your keys from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_live_xxx

# Optional: Stripe webhook secret (for production webhook handling)
# STRIPE_WEBHOOK_SECRET=whsec_xxx

# Optional: Twilio (SMS Notifications)
# ================================
# Get credentials from: https://console.twilio.com/
# TWILIO_ACCOUNT_SID=ACxxx
# TWILIO_AUTH_TOKEN=xxx
# TWILIO_PHONE_NUMBER=+1234567890

# Optional: Object Storage (if using cloud storage)
# ================================
# For Google Cloud Storage or similar
# PUBLIC_OBJECT_SEARCH_PATHS=gs://your-bucket/public
# PRIVATE_OBJECT_DIR=gs://your-bucket/private

# Optional: Monitoring & Analytics
# ================================
# Sentry DSN (error tracking)
# SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# PostHog API Key (product analytics)
# POSTHOG_API_KEY=phc_xxx
# POSTHOG_HOST=https://app.posthog.com
